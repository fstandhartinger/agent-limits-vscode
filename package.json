{
  "name": "claude-limits",
  "displayName": "Claude, Codex & Devin Limits",
  "description": "Monitor Claude Code, Codex, and Devin usage in separate VS Code status bar indicators.",
  "version": "0.5.0",
  "publisher": "maslovserg",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/fstandhartinger/agent-limits-vscode"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "claudeLimits.refresh",
        "title": "Agent Limits: Refresh"
      }
    ],
    "configuration": {
      "title": "Agent Limits",
      "properties": {
        "claudeLimits.language": {
          "type": "string",
          "enum": [
            "ru",
            "en"
          ],
          "enumDescriptions": [
            "Russian: Сессия / Неделя",
            "English: Session / Week"
          ],
          "default": "en",
          "description": "Language for status bar labels"
        },
        "claudeLimits.claudeVisibility": {
          "type": "string",
          "enum": [
            "auto",
            "always",
            "hidden"
          ],
          "enumDescriptions": [
            "Show only when signed in to Claude",
            "Always show the Claude indicator",
            "Always hide the Claude indicator"
          ],
          "default": "auto",
          "description": "Controls Claude limits visibility"
        },
        "claudeLimits.codexVisibility": {
          "type": "string",
          "enum": [
            "auto",
            "always",
            "hidden"
          ],
          "enumDescriptions": [
            "Show only when signed in to Codex",
            "Always show the Codex indicator",
            "Always hide the Codex indicator"
          ],
          "default": "auto",
          "description": "Controls Codex limits visibility"
        },
        "claudeLimits.devinVisibility": {
          "type": "string",
          "enum": [
            "auto",
            "always",
            "hidden"
          ],
          "enumDescriptions": [
            "Show when a Devin quota source is configured (local summary file, SSH host, or API key file)",
            "Always show the Devin indicator",
            "Always hide the Devin indicator"
          ],
          "default": "auto",
          "description": "Controls Devin visibility"
        },
        "claudeLimits.devinSshHost": {
          "type": "string",
          "default": "sandy",
          "description": "SSH host that publishes the Devin quota summary (~/.local/state/agent-limits/devin-latest.json). Read with `ssh <host> cat ...` using key-based auth; on Windows `wsl.exe ssh` is tried when Windows ssh fails. Leave empty to disable. Not used when the file exists locally."
        },
        "claudeLimits.showProgressBars": {
          "type": "boolean",
          "default": true,
          "description": "Show progress bars for Claude and Codex. Disable to save status bar space; percentages and reset times remain visible."
        }
      }
    },
    "icons": {
      "claude-limits-claude": {
        "description": "Claude service logo",
        "default": {
          "fontPath": "./resources/service-icons.woff",
          "fontCharacter": "\\E001"
        }
      },
      "claude-limits-codex": {
        "description": "Codex service logo",
        "default": {
          "fontPath": "./resources/service-icons.woff",
          "fontCharacter": "\\E002"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^2.22.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
